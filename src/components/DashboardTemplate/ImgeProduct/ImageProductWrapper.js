import styled from "styled-components";

const ImgeProductWrapper = styled.img `
    max-width: 600px;
    width: 90%;
    display: block;
    border-radius: 10px;
    margin: 10px auto;
`;

export default ImgeProductWrapper;